const INITIAL_STATE = {

}

const authReducer = (state = INITIAL_STATE, action: {type: string, data: any}): any  => {
    switch (action.type) {
        default:
            return state
    }
}

export default authReducer;